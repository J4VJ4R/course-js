let result = (385 % 2 == 0) ? "es par" : "es impar";
console.log(result)

let miNumero = "18d"

let edad = Number(miNumero)

if(isNaN(edad)){
  console.log('no es un múmero')
}else{
  let voto = (edad >= 18) ? "puede votar" : "no puede votar";
}

console.log(voto)

//verificando que una variable sea de tipo número
