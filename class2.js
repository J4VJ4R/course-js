let name = "Javier", lastname = "Jaramillo";
let nameMayus = name.toUpperCase();
let lastnameLow = name.toLowerCase();
let quantityLetters = name.length;
let fullName = `${name.toLowerCase()} ${lastname.toUpperCase()}`;
let str = name.substr(1, 3);
